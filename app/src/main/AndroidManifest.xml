<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.getjson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Retrofit.Activities.GetById.TODOsByUserIdActivity"></activity>
        <activity android:name=".Retrofit.Activities.GetById.AlbumsByUserIdActivity" />
        <activity android:name=".Retrofit.Activities.GetById.CommentsByPostIdActivity" />
        <activity android:name=".Retrofit.Activities.GetList.TODOsListActivity" />
        <activity android:name=".Retrofit.Activities.GetList.PhotosListActivity" />
        <activity android:name=".Retrofit.Activities.GetList.AlbumsListActivity" />
        <activity android:name=".Retrofit.Activities.GetById.PhotosByAlbumIdActivity" />
        <activity android:name=".Retrofit.Activities.ShimmerFacebookRV" />
        <activity android:name=".Retrofit.Activities.GetById.PostsByUserIdActivity" />
        <activity android:name=".Retrofit.Activities.GetList.CommentsListActivity" />
        <activity android:name=".Retrofit.Activities.GetList.UsersListActivity" />
        <activity android:name=".Retrofit.Activities.GetList.PostsListActivity" />
        <activity android:name=".Retrofit.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>